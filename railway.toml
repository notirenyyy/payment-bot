[build]
builder = "nixpacks"

# Bot is now fully managed by GitHub Actions (poll-callbacks every 5 min,
# weekly-reminder Sunday 4am UTC). Railway is no longer used — you can
# pause or delete this service to stop incurring costs.
